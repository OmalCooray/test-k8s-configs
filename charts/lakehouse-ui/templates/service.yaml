apiVersion: v1
kind: Service
metadata:
  name: lakehouse-ui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lakehouse-ui
  ports:
    - name: http
      port: 8000
      targetPort: http
